// Configuration file for blog settings
const CONFIG = {
    BLOG_PASSWORD: '270412'
};

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG;
}
